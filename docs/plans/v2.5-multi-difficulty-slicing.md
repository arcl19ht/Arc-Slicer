# V2.5 多难度切片

## 背景与用户价值

当前 Arc Slicer 固定处理歌曲目录中的 `2.aff`，并为每个时间片段生成一个 FTR 兼容输出。多难度切片的目标是在不破坏既有单难度导出、音频复用和外部目标壳安全边界的前提下，让一个片段目录能够包含用户选择的多个难度 AFF。

本计划是实施前的现行规划，不替代历史需求书。涉及目标壳的难度文件名、编号和 `ratingClass` 的事实必须先从真实样本和兼容要求审计确认，不能在规划阶段伪造结论。

## 当前事实与不变原则

- 当前默认且唯一正式输入为 `2.aff`；旧 slides 继续默认使用它。
- 既有片段 ID 保持不变：`<source_song_id>_<start_ms>_<end_ms>_x<speed_token>`。
- 一个片段输出目录可以包含多个难度 AFF。
- 对同一片段和倍速，`base.ogg` 必须只切一次，不得因难度重复运行 ffmpeg。
- 不改变 AFF 切片语义、`current_export` / `library_export` 结构或 external merge 的安全边界。

## V2.5-A：多难度基础与兼容模型

本阶段只审计和设计，不实现多难度导出界面或切片流程。

1. 审计实际支持的 AFF 文件名、难度编号、`ratingClass` 和目标壳 songlist 格式。
2. 设计难度发现与用户多选模型，并说明空选择、部分选择、缺失文件和未知难度的行为。
3. 设计 slides 持久化字段与迁移：旧 slides 缺失新字段时必须等价于仅选择 `2.aff`。
4. 设计多难度 `songlist.difficulties` 数据模型及兼容策略。
5. 明确需由真实歌曲、外部测试壳和格式样本确认的事实；不得假定难度只支持 0–3。

## V2.5-B：选择、持久化与多 AFF 导出

在 V2.5-A 审计结论确认后实施：

- 难度选择 UI 与持久化。
- 选中 AFF 的发现、校验、切片和写入同一片段目录。
- 多难度 songlist difficulties 生成。
- 单次音频切片复用给该片段的所有难度。

## V2.5-C：兼容与验收

- 覆盖缺失难度、未知难度和部分难度选择。
- 使用真实歌曲与外部测试壳验证导出和 external merge 边界。
- Windows 源码与 EXE 人工验收。

## 数据与兼容约束

- 难度发现结果应区分已知、未知、缺失和不可读文件，不能静默丢弃用户选择。
- slides 新字段必须有明确版本兼容与迁移规则，旧保存数据不能因升级失效。
- `speed_override`、片段区间、`uid`、`link_group_id` 与多难度选择保持正交。
- 同一片段内每个难度共享同一时间边界和切后音频，但各自保留自己的 AFF 内容。

## 外部合并边界

V2.5 不放宽 external merge 的目录验证、计划、确认、备份、manifest 或失败恢复规则。多难度输出只改变合法 staging 歌曲目录中的内容，不得改变外部合并的安全模型。

## 测试计划

- 单难度旧 slides 的默认兼容行为。
- 难度发现和未知/缺失难度策略。
- 多 AFF 输出与单次 ffmpeg 调用。
- 多难度 songlist difficulties。
- current/library export 与 external merge 回归。
- JSON 迁移、撤销/重做和 source switch 回归。

## 人工验收计划

- 真实歌曲的多难度发现和部分选择。
- 输出目录中多个 AFF 与单份 `base.ogg`。
- 目标壳 songlist 与外部合并兼容。
- Windows EXE 导出与导入。

## 非目标

- Combo snapping：已取消。
- 谱面预览：远期候选，不在 V2.5-A/B/C 中承诺。
- 第二播放器或 ffmpeg 临时试听音频。
- DAW 工作流。
